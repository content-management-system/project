@model IEnumerable<cmsProject.Models.Content>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="row justify-content-md-center">
    <div class="form-group col-md-6">
        <a asp-controller="Home" asp-action="AdminDashboard" class="btn-lg btn-warning btn-block">Back</a>
    </div>
    <div class="form-group col-md-6">
        <a asp-controller="Content" asp-action="Create" class="btn-lg btn-success btn-block">New Post</a>
    </div>

</div>
@*<div class="row justify-content-md-center">
    <div class="form-group col-md-6">
        <a asp-controller="Content" asp-action="Create" class="btn-lg btn-success btn-block">New Post</a>
    </div>
    <div class="form-group col-md-2 text-center">
        <a asp-controller="Home" asp-action="AdminDashboard" class="btn-lg btn-warning btn-block">Back</a>
    </div>

@*<p>
    <a asp-action="Create" class="btn btn-outline-success"><i class="far fa-plus-square"></i> Create New</a>
</p>*@
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContentHeader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContentBody)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContentHeader)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContentBody)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContentId">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.ContentId">Delete</a> |
                <a asp-action="Details" asp-route-id="@item.ContentId">View</a>
                
            </td>
        </tr>
}
    </tbody>
</table>
